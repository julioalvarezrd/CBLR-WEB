import { requirePagePermission } from "@/modules/auth/permissions/page-authorization"; import { ContentPanel } from "@/components/ui/content-panel"; import { ModuleHeader } from "@/components/ui/module-header";
import { createRankAction } from "@/modules/institutional-catalog/catalog.actions"; import { listRanks } from "@/modules/institutional-catalog/catalog.service"; import { CatalogCreateForm } from "@/modules/institutional-catalog/components/catalog-create-form"; import { CatalogTable } from "@/modules/institutional-catalog/components/catalog-table";
export default async function RanksPage() { const context = await requirePagePermission("catalogo.view"); const items = await listRanks(); const fields = [{ name: "name", label: "Nombre", required: true, maxLength: 120, placeholder: "Nombre del rango" }, { name: "category", label: "Categoría", required: true, maxLength: 80, placeholder: "Ej. Oficiales" }, { name: "hierarchy", label: "Orden jerárquico", type: "number" as const, required: true }] as const; return <div className="space-y-7"><ModuleHeader eyebrow="Catálogo institucional" title="Rangos" description="Escala jerárquica institucional utilizada por Personal." stats={[{ label: "Total", value: items.length }, { label: "Activos", value: items.filter((item) => item.isActive).length }]} /><ContentPanel title="Nuevo rango"><CatalogCreateForm action={createRankAction} fields={fields} /></ContentPanel><ContentPanel title="Escala de rangos"><CatalogTable entity="rank" canManage={context.permissions.has("catalogo.manage")} items={items} emptyMessage="No hay rangos registrados." editFields={(item) => fields.map((field) => ({ ...field, value: item[field.name as "name" | "category" | "hierarchy"] }))} columns={[{ label: "Orden", render: (item) => item.hierarchy }, { label: "Rango", render: (item) => <span className="font-semibold text-slate-950 dark:text-white">{item.name}</span> }, { label: "Categoría", render: (item) => item.category }]} /></ContentPanel></div>; }